# V0.7 工作记录 - 2026-07-18

## 分支与已推送提交

- 文档基线：`codex/v0.7-commercial-foundation`，`02252143961c0c69b29fbb111be5d325e8f5c7f2`。
- M1 分支：`codex/v0.7-m1-commercial-foundation`，已推送提交 `ccfcb22a1eafa892e2e8c90f1a72304bd8b17f0e`。
- 当前分支：`codex/v0.7-m2-quota-cloud`。
- M2 服务端阶段性提交：已推送 `ba9b5161b12ecf6878f58dd375e8627945ee130f`。

不要切回 `main`，不要修改 `codex/v0.6.4-elevation-insertion`，不要操作 D 盘 v0.6.4 程序。

## M1 已完成

- 新增 ASP.NET Core 10 `Zumingtalk.Service` 和 `Zumingtalk.ServiceTests`。
- 新增 PostgreSQL Docker Compose、可重复的 SQL 初始化脚本、环境变量示例与 M1 运行说明。
- 激活邀请码、单设备绑定、SHA-256 哈希化设备令牌、管理员邀请/重置/Pro/加量包和无敏感信息审计已完成。
- 管理员密钥只从服务端配置读取；邀请码、令牌、数据库密码和服务端百炼 Key 没有提交。

## M2 当前进度

已提交并推送的服务端部分：

- `QuotaSessionService`：可用余额预留，上限 600 秒，实际可预留的短余额也允许开始会话。
- 16 kHz、16-bit、单声道 PCM 按服务端收到的字节数计量；月度桶优先、加量包其次；失败/取消释放预留；完成结算具备幂等账本记录。
- `POST /api/asr/sessions`、`GET /api/asr/stream` 和 `POST /api/asr/sessions/{id}/finish` 已实现。WSS 双向中转服务端百炼，未持久化 PCM 或识别正文。

未提交的 M2 客户端工作树：

- 默认识别来源改为“祖名云端识别”，只有用户明确选择“自有百炼 Key”才读取现有 DPAPI Key。
- 新增 DPAPI 设备令牌、随机匿名设备安装标识和云端激活/权益 HTTP 客户端。
- 新增真实云端 ASR HTTP/WSS 提供器，识别异常与取消会调用服务端 finish 释放预留。
- 新增“订阅与额度”WPF 页面、激活入口和额度刷新；页面不显示服务端百炼 Key 或设备令牌。
- 增加反向可见性转换器；已修复 Release 编译错误。

这些未提交修改必须在明天继续前审阅、补单元测试和真人服务环境验证后，作为完成的 M2 提交单独推送。不得把当前未提交改动描述为 M2 已完成。

## 已验证

2026-07-18 收尾命令：

```powershell
dotnet build Zumingtalk.sln -c Release --no-incremental
dotnet test Zumingtalk.sln -c Release --no-build
git diff --check
```

结果：Release 构建 `0 warning / 0 error`；测试 `54 passed / 0 failed`，其中客户端原有测试 48 项、服务端商业 API/额度测试 6 项；`git diff --check` 通过。

## 明日续做顺序

1. 先阅读本文件、`docs/HANDOFF-V0.7-COMMERCIALIZATION.md`、`docs/prd/PRD-002-commercialization.md`，并检查工作树不被其他人改写。
2. 为 DPAPI 云端令牌存储、识别来源持久化、激活/权益失败提示和云端会话释放补测试。
3. 用 Docker Desktop 启动 PostgreSQL；当前机器 2026-07-18 未安装 Docker CLI，不能把 SQLite 集成测试当作 PostgreSQL 迁移实测。
4. 配置仅本机环境变量 `ConnectionStrings__Zumingtalk`、`Service__AdminApiKey`、`Bailian__ApiKey`，完成真实邀请码激活和真实服务端百炼转写。不得提交 `.env`、令牌、录音、数据库、日志或真实 Key。
5. 用真实麦克风复测记事本、Codex、微信、浏览器、VS Code 和 360 开关矩阵，确认 M2 没有回归 v0.6.4 的热键、胶囊、录音、历史和分层写入。
6. 只有完成上述自动测试与真人/服务端证据后，才提交 `feat(M2): connect desktop cloud recognition and quota UI` 并推送当前分支。

## 当前风险

- Docker PostgreSQL 和真实服务端百炼中转尚未在本机执行，因为 Docker CLI 不存在、服务端百炼 Key 未配置。这是未完成的真人/真实环境验证，不是自动测试通过。
- M3 的火山真实适配器、Pro 自有 Key 门禁、设置重整、FAQ/反馈尚未开始。
- M4 支付宝沙箱、验签和退款状态尚未开始。
- M5 的连续 10 小时真实成本标定不能提前宣称完成。
